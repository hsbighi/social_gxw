spring:
  application:
    name: order #microservice-eureka-order

server:
  port: 7900  #指定该服务的端口号
eureka:
  instance:
    prefer-ip-address: true # 是否显示主机IP
  client:
    service-url:
      defaultZone:  http://localhost:8761/eureka/ #指定eureka注册中心地址
